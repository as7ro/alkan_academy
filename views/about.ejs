<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Müraciət Məlumatları</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
        }
        .container {
            max-width: 60%;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }
        h2 {
            text-align: center;
            color: #007bff;
        }
        p {
            font-weight: bold;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h2>Müraciət Məlumatları</h2>
        <div>
            <p>Ad Soyad: <span id="adSoyad"></span></p>
            <p>Cins: <span id="cinsiyet"></span></p>
            <p>Vətəndaşlıq: <span id="vatandaslik"></span></p>
            <p>Ünvan: <span id="unvan"></span></p>
            <p>Doğum Tarixi: <span id="dogumTarihi"></span></p>
            <p>Sürücülük vəsiqəsi: <span id="ehliyet"></span></p>
            <p>E-mail ünvanı: <span id="email"></span></p>
            <p>Telefon nömrəsi: <span id="mobile"></span></p>
            <!-- Diğer alanları da aynı şekilde ekleyebilirsiniz -->
        </div>
    </div>

    <!-- Bootstrap JS ve jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/js/bootstrap.min.js"></script>

    <script>
        // jQuery kullanarak formdan alınan verileri gösterme
        $(document).ready(function(){
            $("form").submit(function(event){
                event.preventDefault();
                
                // Her bir input alanının değerini alarak ilgili span'e yazdırma
                $("#adSoyad").text($("input[name='adSoyad']").val());
                $("#cinsiyet").text($("select[name='cinsiyet']").val());
                $("#vatandaslik").text($("select[name='vatandaslik']").val());
                $("#unvan").text($("input[name='unvan']").val());
                $("#dogumTarihi").text($("input[name='dogumTarihi']").val());
                $("#ehliyet").text($("input[name='ehliyet']:checked").val());
                $("#email").text($("input[name='email']").val());
                $("#mobile").text($("input[name='mobile']").val());
                // Diğer alanları da aynı şekilde ekleyebilirsiniz
            });
        });
    </script>
</body>
</html>